<script lang="ts">
  interface Props {
    size?: 'sm' | 'md' | 'lg';
    class?: string;
  }

  let { size = 'md', class: className = '' }: Props = $props();

  const sizeClasses = {
    sm: 'w-4 h-4 border-2',
    md: 'w-6 h-6 border-2',
    lg: 'w-8 h-8 border-3'
  };
</script>

<div
  class="border-accent border-t-transparent rounded-full animate-spin {sizeClasses[size]} {className}"
  role="status"
  aria-label="Loading"
></div>
